name: 'Run script'

inputs:
  AUTH_TOKEN:
    description: 'github token'
    required: true
  ORGANISATION:
    description: 'github token'
    required: true
  REPOSITORIES:
    description: 'github token'
    required: true
runs:
  using: 'node16'
  main: 'build/index.js'

